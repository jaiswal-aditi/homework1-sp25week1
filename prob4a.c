#include <stdio.h>

/*
Read two integers and two floating point numbers, each in a separate scanf()
statement (so 4 scanf’s) and print them with separate printf statements (4 printf’s)
with some nice message.
*/

int main() {
    // your code
    return 0;
}
